T-036 truthful crew progress — worker round notes

## What landed

- Mid-run contract documented in `design/design.md` (phase / authored
  `data.activity` / optional bounded `{done,total}`; prototype random pct is
  demo-only).
- `crew_status` event type on `fm-emit.sh` with content-aware coalesce:
  identical heartbeats throttle; changed activity or progress always writes.
- Board `/api/state` exposes authored activity, `crew_name`, truthful phase,
  and bounded progress only (bare numbers refused). Client bars (bubble +
  roster) only when bounded progress is present; no stage→pct map.
- Producers (`fm-worker.sh`, `fm-review.sh`, `bin/fm-herdr.py emit-status`)
  emit phase/activity via `fm-emit.sh` at script-known nodes. No vendor-only
  percent invention; heartbeat pane text is not board state until emitted.

## Evidence (observed this session)

Fail-first (before throttle fix): board suite showed
`bounded done/total … FAIL expected [{"done":3,"total":7}] got [null]` and
`roster … FAIL` (cross-`<li>` regex false positive).

After fixes (commands run locally):

- `bash tests/board.test.sh` — T-036 block all ok (activity round-trip,
  bare progress refused, bounded progress, phase retention, no bar without
  bound, roster bar only with bound).
- `bash tests/emit.test.sh` — crew_status coalesce ok.
- `bash tests/herdr.test.sh` — emit-status activity/progress ok.
- `bash tests/session.test.sh` — contract markers ok.
- `bash tests/worker.test.sh` — exit 0; crew_status asserts ok.
- `bash tests/review.test.sh` — exit 0; crew_status assert ok.
- `FM_CI_MAX_SECONDS=600 bash bin/ci.sh` — result recorded when finished.

## Scope note for captain

`bin/fm-checkpoint.sh` is present on this worktree and wired from
`fm-worker.sh` / `tests/worker.test.sh` / worker skill (concurrent mid-run
save work). It is **not** in T-036's declared scope globs. Gate 4 may need a
captain widen or a split. This round did not invent that file; it only
aligned the worker checkpoint unit tests with the script's `--task` API so
the suite is green.

## Gate 5

New T-036 asserts (board/emit/herdr) target `crew_status`, bounded progress
parsing, and coalesce — they go red if those implementation hunks are
reverted. Not waived.
